# prumo-web-client

O app web principal está atualmente na raiz deste repositório (`src/`, `supabase/`, `package.json`).

Esta pasta existe como marcador para o split futuro em repositório dedicado.

